import { ReactNode } from "react";
import { Button, Text } from '@chakra-ui/react';

interface Props {
  isLoading?: boolean;
  type?: "button" | "submit" | "reset" | undefined;
  children?: ReactNode
}
const ButtonComponent = ({ isLoading, type, children }: Props) => {
  return }

export default ButtonComponent;
