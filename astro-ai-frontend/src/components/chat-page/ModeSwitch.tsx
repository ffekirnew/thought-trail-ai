import { Button, HStack } from "@chakra-ui/react";
import { useState } from "react";
import { useNavigate } from "react-router-dom";

interface Props {
  options: string[]
}
const ModeSwitch = ({ options }: Props) => {
  
  return ();
}

export default ModeSwitch
